# Checkpoint: Kinemation K001+K002+K004 Fixes Applied
**Date:** 2026-02-28
**System:** PlayerCharacter / Kinemation
**Branch:** main (HEAD)
**Status:** CHANGES DEPLOYED — awaiting PIE test

---

## What Was Done

### K001: Fire Input Wired
- Created `IA_Fire` (Boolean) at `/Game/Core/Input/Actions/IA_Fire`
- Added `LeftMouseButton → IA_Fire` to IMC_Grace (now 11 total mappings)
- Set `FireAction` on BP_GraceCharacter CDO
- Added `Input_FireStarted` / `Input_FireCompleted` handler declarations in ZP_GraceCharacter.h
- Added binding in `SetupPlayerInputComponent()` (Started + Completed triggers)
- Handlers call `FKinemationBridge::WeaponOnFirePressed/Released(ActiveWeapon)`

### K002: FPP Mesh Swap
- Swapped PlayerMesh from `SKM_Operator_Mono` (full body) to `SK_Shirt_01a_FPP` (arms-only)
- Reset PlayerMesh RelativeLocation from (0,0,-152) to (0,0,0) — FPP mesh designed for camera level
- RelativeRotation kept at (P=0, Y=-90, R=0)
- AnimBP unchanged: `ABP_TacticalShooter_UE5` (was set in previous session)

### K003: No Action (by design)
- Lower body visibility not needed for prototype
- Moot after FPP mesh swap — there's no lower body geometry to show

### K004: Shadow-Only Body Mesh
- Configured inherited `Mesh` component (from ACharacter) as shadow-only:
  - `bHiddenInGame = true`
  - `CastShadow = true`
  - `bCastDynamicShadow = true`
  - `bCastHiddenShadow = true`
  - `bOnlyOwnerSee = false`
  - `VisibilityBasedAnimTickOption = AlwaysTickPoseAndRefreshBones`
- Set SkeletalMesh to `SKM_Operator_Mono` (full body)
- Shadow will be in T-pose until GASP AnimBP is added (acceptable for prototype)
- This mesh becomes the GASP motion-matching mesh in future integration

---

## Build Result
- **COMPILED CLEAN**: 4 actions, 3.2s, 0 errors
- Fix needed: `SetVisibilityBasedAnimTickOption()` doesn't exist in UE 5.4 — changed to direct property assignment `VisibilityBasedAnimTickOption = ...`

## Python Scripts Executed
1. `fix_k002_k004.py` — mesh swap + shadow body setup ✓
2. `setup_fire_input.py` — IA_Fire creation + IMC mapping + CDO binding ✓

## Verified CDO State
- PlayerMesh SkeletalMeshAsset: SK_Shirt_01a_FPP ✓
- Inherited Mesh SkeletalMeshAsset: SKM_Operator_Mono ✓
- FireAction: IA_Fire ✓

---

## Files Modified

| File | Changes |
|------|---------|
| `ZP_GraceCharacter.h` | +Input_FireStarted, +Input_FireCompleted declarations |
| `ZP_GraceCharacter.cpp` | +Shadow mesh config in constructor, +FireAction binding in SetupPlayerInputComponent, +Fire handler implementations, +Fire in log line |
| `Scripts/Python/fix_k002_k004.py` (NEW) | Mesh swap + shadow body Python automation |
| `Scripts/Python/setup_fire_input.py` (NEW) | Fire input asset creation + mapping |
| `BP_GraceCharacter` (via Python) | PlayerMesh→SK_Shirt_01a_FPP, Mesh→SKM_Operator_Mono, FireAction→IA_Fire |
| `IA_Fire` (NEW) | Boolean input action at /Game/Core/Input/Actions/IA_Fire |
| `IMC_Grace` (updated) | +LeftMouseButton→IA_Fire (11 total mappings) |

---

## PIE Test Expectations

1. **K002 — Arms visibility:** FPP arms should be visible holding TR15 rifle. No torso/body clipping. If arms are invisible or wrong position, tune Z offset.
2. **K004 — Shadow:** Full-body shadow on ground (T-pose). If no shadow, check `bCastHiddenShadow` compatibility with Virtual Shadow Maps.
3. **K001 — Fire:** LMB should trigger weapon fire. Expect: recoil animation, camera shake, muzzle flash, sound, ammo decrement. If no fire: check logs for FireAction binding status.

## Risks / Possible Issues

| Risk | Mitigation |
|------|------------|
| FPP mesh at Z=0 may show arms too high/low | Tune Z offset after PIE observation |
| Socket "VB ik_hand_gun" may not exist on FPP mesh skeleton | Will show as detached weapon — check socket list |
| ABP_TacticalShooter_UE5 may not drive FPP mesh correctly | Different bone set — if arms invisible, fall back to SKM_Operator_Mono with adjusted offset |
| bCastHiddenShadow + VSM incompatibility | Test with both shadow systems |
| T-pose shadow looks odd | Acceptable for prototype — GASP will fix |
